{
  "name": "zempel-auto-frontend",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p dist && cp -r index.html sw.js manifest.json assets dist/",
    "build:win": "powershell -Command \"New-Item -ItemType Directory -Force -Path dist | Out-Null; Copy-Item index.html,sw.js,manifest.json -Destination dist; Copy-Item -Recurse -Force assets dist/\"",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.2.4"
  }
}
