{
  "name": "zempel-auto-frontend",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p dist && cp -r index.html manifest.json _headers *.js assets dist/",
    "build:win": "powershell -Command \"New-Item -ItemType Directory -Force -Path dist | Out-Null; Copy-Item index.html,manifest.json,_headers,*.js -Destination dist; Copy-Item -Recurse -Force assets dist/\"",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.2.4"
  }
}
